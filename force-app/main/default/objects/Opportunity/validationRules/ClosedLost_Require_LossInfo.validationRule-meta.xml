<?xml version="1.0" encoding="UTF-8" ?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ClosedLost_Require_LossInfo</fullName>
    <active>true</active>
    <description
  >Require Loss Reason when StageName is Closed Lost. Loss Reason Details is only required when Loss Reason is "Other" (enforced by a separate rule).</description>
    <errorConditionFormula>AND(
  ISPICKVAL(StageName, &quot;Closed Lost&quot;),
  ISBLANK(TEXT(Loss_Reason__c))
)</errorConditionFormula>
    <errorDisplayField>Loss_Reason__c</errorDisplayField>
    <errorMessage
  >When Stage is Closed Lost, a Loss Reason must be selected.</errorMessage>
</ValidationRule>
